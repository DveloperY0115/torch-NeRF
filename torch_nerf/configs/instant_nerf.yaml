defaults:
  - cuda: default
  - data: nerf_synthetic
  - network: instant_nerf
  - objective: nerf
  - train_params: nerf
  - scene: cube
  - renderer: volume_renderer_default
  - signal_encoder: positional_encoding

renderer:
  num_samples_fine: 0  # InstantNeRF does not require hierarchical sampling